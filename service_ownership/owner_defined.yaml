---
kind: has_owner
spec:
    category: quality
    enabled: true
    # uncomment if you want to apply a filter
    # filter: tier-4-or-above
    level: bronze
    name: Owner Defined
    notes: All services need ownership; it helps ensure authority, autonomy, and accountability.
    # uncomment if you want to apply a check owner
    # owner: sre_team
